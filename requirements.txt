# Pinned exactly: the .joblib artifacts in model/ were pickled with these two,
# and a mismatched version is the usual cause of a broken Streamlit Cloud deploy.
scikit-learn==1.6.1
joblib==1.5.3

streamlit>=1.49
pandas>=2.0
numpy>=1.26,<3
matplotlib>=3.8
seaborn>=0.13
